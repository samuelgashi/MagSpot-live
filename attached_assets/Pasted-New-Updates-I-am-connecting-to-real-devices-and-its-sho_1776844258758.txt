New Updates:
- I am connecting to real devices and its showed on magSpot2.0 dashboard but live view not available. it says: :5173/api/devices/stream?deviceId=192.168.1.38%3A5555&fps=2&t=1776841106831:1  Failed to load resource: the server responded with a status of 404 (NOT FOUND). I think its bascause its using :5173? not PROXY_API_URL?
- On MagSpot2.0 Dashboad Activities,section can you match the inputs with inputs got from activities from inputs? On old magSpot i was creating the inputs objects dynamically from activities arguments and kwargs. You can check the magSpot frontend for that. Implement that same here. One good thing on magSpot2.0 frontend activities it verifies the inputs and show the message, keep that there with new activities settings. Don't change the activities args, kwargs, names it is connected to backend/app/automation/handlers/stream_worker_node.py and backend/app/automation/activities/*.py
- Turing off the "Small Screen" should not close the live view, it should make it user don't click, drag or swipe that the small android screen on dashboard. [but can with android large display view when clicking on it]
- Check magSpot frontend, you can see on "Devices Action" there is "Connect Devices" box, i want that "Restart ADB" button into magSpot2.0 "ADB command" popup box.
- Check the magSpot2.0 "device" popup, you can see there is multiple inputs boxes, as per devices. You have to check if there is respective database in magSpot backend? if no create database tables in models and connect with frontend each device.
- We have to save each device, even if got from adb or not [maybe on first it finds and later its not] but its data might be saved in database right? Check my magSpot backend, and see if devices are saving in database and keep's there so that device ID can we used as primary key as a relation on other tables?
- Add a filter or radio button on magSpot2.0 frontend to show only online devices not online, also if we click the group from slidebar its should only select the live devices not offline.